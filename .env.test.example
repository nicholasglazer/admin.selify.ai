# Playwright Test Configuration
# Copy this file to .env.test and fill in your test credentials

# Option 1: Email/Password Auth (recommended for CI)
# Use a test user that exists in internal.team_members with status='active'
TEST_USER_EMAIL=admin@selify.ai
TEST_USER_PASSWORD=your-password-here

# Option 2: Direct Auth Token (for advanced use)
# Get a valid Supabase access token and set it here
# TEST_AUTH_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Base URL for tests (optional - defaults to http://localhost:5174)
# BASE_URL=http://localhost:5174

# Supabase Configuration (for test user creation/cleanup)
# PUBLIC_SUPABASE_URL=https://api.selify.ai
# PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# CI Configuration
# CI=true  # Set automatically in GitHub Actions
